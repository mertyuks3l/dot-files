# Keybinds for my setup

# Constants
$scriptsDir = ~/.config/hypr/scripts

$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hypr.land/Configuring/Binds/ for more
bind = $mainMod, Q, killactive
bind = $mainMod, Return, exec, $terminal
bind = $mainMod, X, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, Space, exec, $menu
bind = $mainMod, E, togglesplit, # dwindle
bind = $mainMod, F, fullscreen
bind = Ctrl Alt, Delete, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Move windows with mainMod + SHIFT + arrow keys
bind = $mainMod SHIFT, left, movewindow, l
bind = $mainMod SHIFT, right, movewindow, r
bind = $mainMod SHIFT, up, movewindow, u
bind = $mainMod SHIFT, down, movewindow, d

# Resize windows with mainMod + CTRL + Arrow keys
binde = $mainMod CONTROL, right, resizeactive, 30 0
binde = $mainMod CONTROL, left, resizeactive, -30 0
binde = $mainMod CONTROL, up, resizeactive, 0 -30
binde = $mainMod CONTROL, down, resizeactive, 0 30

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Scrol through existing windows with mainMod + Tab
bind = $mainMod, Tab, workspace, e+1
bind = $mainMod SHIFT, Tab, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = , XF86AudioRaiseVolume, exec, $ipc volume increase
bindel = , XF86AudioLowerVolume, exec, $ipc volume decrease
bindl = , XF86AudioMute, exec, $ipc volume muteOutput
bindel = , XF86MonBrightnessUp, exec, $ipc brightness increase
bindel = , XF86MonBrightnessDown, exec, $ipc brightness decrease

# Media controls
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# Power save (enable/disable animations, rounded corners, blur)
bind = $mainMod Shift, B, exec, $scriptsDir/powertoggle.sh

# Focus mode (enable/ disable animations)
bind = $mainMod Shift, F, exec, $scriptsDir/focustoggle.sh

# Touchpad
bind = $mainMod Shift, T, exec, $scriptsDir/toggle-touchpad.sh

# Flameshot - For screenshot
bind = ,Print, exec, flameshot gui

# Keybinds for idk
bind = $mainMod, Escape, exec, $ipc controlCenter toggle

bind = $mainMod, L, exec, $ipc lockScreen lock 

bind = $mainMod Shift, L, exec, $ipc sessionMenu toggle

bind = $mainMod, W, exec, $ipc wallpaper toggle

bind = $mainMod Shift, W, exec, $ipc wallpaper random all 

# Custom binds for my setup
bind = $mainMod, B, exec, $browser # for spotify

bind = $mainMod, C, exec, $browser --app=https://claude.ai # for claude 

bind = $mainMod, G, exec, $browser --app=https://gemini.google.com # for gemini

bind = $mainMod, P, exec, spotify # for spotify

bind = $mainMod, Y, exec, $browser --new-window https://youtube.com

bind = $mainMod Shift, Y, exec, $browser --app=https://music.youtube.com

bind = $mainMod, D, exec, $browser https://drive.google.com/drive/my-drive # for google drive

bind = $mainMod, M, exec, $browser --app=https://mail.google.com/mail/u/0/\#inbox # for mail

bind = $mainMod Shift, M, exec, $browser --app=https://mail.google.com/mail/u/1/\#inbox # for mail other account
